The .env file contains credentials and urls used in the app.
The app uses the doctor key as the channel for communication between patient and doc.
The app uses keys to relate to each other e.g to get all doctors are categroized under their respectives hostpitals keys in the doctor collection and all donation has the key of doctor respectively 
some functions or methods are self explanatory
